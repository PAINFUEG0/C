{
  "tasks": [
    {
      "type": "shell",
      "args": ["${file}"],
      "problemMatcher": ["$gcc"],
      "command": "${workspaceFolder}\\run.bat",
      "group": { "kind": "test", "isDefault": true },
      "label": "compile current file and run executable"
    }
  ],
  "version": "2.0.0"
}
